# =============================================================================
# NoteNest - Umgebungsvariablen (Development)
# =============================================================================
# Kopiere diese Datei zu .env und passe die Werte an
# WICHTIG: .env wird NICHT committed (steht in .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# JWT Secrets (WICHTIG: Sichere Werte generieren!)
# -----------------------------------------------------------------------------
# Automatisch generiert durch:
# - scripts/setup-env.sh (lokal)
# - scripts/docker-entrypoint.js (Docker)
# Oder manuell generieren (PowerShell):
# [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=your-super-secret-jwt-key-here
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here

# -----------------------------------------------------------------------------
# Authentifizierung
# -----------------------------------------------------------------------------
# Modi: local | ldap | synology | hybrid
AUTH_MODE=local
LDAP_ENABLED=false

# -----------------------------------------------------------------------------
# Datenbank
# -----------------------------------------------------------------------------
# Development: Relativer Pfad
# Production: Absoluter Pfad (/data/database/notenest.db)
DB_PATH=./data/database/notenest.db

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
# Hinweis: Docker mappt Ports, der interne Port bleibt 3000.
PORT=3000
NODE_ENV=development
# Wenn ein Reverse Proxy X-Forwarded-For setzt:
# TRUST_PROXY=1

# -----------------------------------------------------------------------------
# Dateisystem
# -----------------------------------------------------------------------------
# Pfade zu Benutzer-Daten (Development: lokal, Production: Container-Pfade)
DATA_ROOT=./data/users
SHARED_DATA_ROOT=./data/shared

# -----------------------------------------------------------------------------
# Bibelstellen-API (Optional)
# -----------------------------------------------------------------------------
# API.Bible (https://scripture.api.bible/)
BIBLE_API_ENABLED=false
BIBLE_API_KEY=
BIBLE_API_URL=https://api.scripture.api.bible

# Bible SuperSearch (Fallback, optional)
BIBLE_SUPERSEARCH_ENABLED=false
BIBLE_SUPERSEARCH_URL=https://api.biblesupersearch.com/api

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Levels: error | warn | info | debug
LOG_LEVEL=debug
LOG_DIR=./logs

# -----------------------------------------------------------------------------
# Frontend (wird automatisch von Vite verwendet)
# -----------------------------------------------------------------------------
VITE_API_URL=http://localhost:3000
