# ============================================
# JWT Secrets (werden automatisch generiert)
# ============================================
# Diese werden beim ersten Start automatisch generiert, wenn sie leer sind
# Du kannst sie auch manuell generieren mit: openssl rand -base64 32
JWT_SECRET=
JWT_REFRESH_SECRET=

# ============================================
# Betriebs- und Sicherheitskonfiguration
# (diese Werte darf der Administrator anpassen)
# ============================================

# Authentifizierungsmodus der Anwendung (kein Secret, aber sicherheitsrelevante Einstellung)
AUTH_MODE=hybrid # Optionen: local | ldap | synology | hybrid # Empfohlen: hybrid (NAS + lokale Benutzer)

# ============================================
# LDAP/NAS Configuration (optional)
# ============================================
# Nur nötig wenn AUTH_MODE ldap, synology oder hybrid ist
LDAP_ENABLED=false
LDAP_URL=ldap://synology-nas.local:389
LDAP_BASE_DN=dc=synology,dc=local
LDAP_BIND_DN=cn=admin,dc=synology,dc=local
LDAP_BIND_PASSWORD=
LDAP_USER_SEARCH_BASE=ou=users,dc=synology,dc=local
LDAP_USER_SEARCH_FILTER=(uid={{username}})

# ============================================
# Bible API Configuration (optional)
# ============================================
# API.Bible API Key (Secret, kann später ergänzt werden)
# Registrierung: https://scripture.api.bible/
BIBLE_API_KEY=
BIBLE_API_URL=https://rest.api.bible
BIBLE_API_ENABLED=true

# Bible SuperSearch (Optional, Fallback)
BIBLE_SUPERSEARCH_ENABLED=true
BIBLE_SUPERSEARCH_URL=https://api.biblesupersearch.com/api

# ============================================
# Rate Limiting (optional)
# ============================================
# Standard-Werte sind im Code konfiguriert; nur bei Bedarf anpassen
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

