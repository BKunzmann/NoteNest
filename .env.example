# JWT Secrets (generiere mit: openssl rand -base64 32)
# WICHTIG: Diese Werte müssen generiert werden, niemals leer lassen in Production!
JWT_SECRET=
JWT_REFRESH_SECRET=

# Authentifizierung
AUTH_MODE=local # local, ldap, synology oder hybrid
LDAP_ENABLED=false
# Für lokale Entwicklung: AUTH_MODE=local, LDAP_ENABLED=false
# LDAP_URL (nur wenn LDAP_ENABLED=true)
# Beispiele:
#   ldap://192.168.1.100:389          # IP-Adresse (unverschlüsselt)
#   ldaps://192.168.1.100:636        # IP-Adresse (verschlüsselt)
#   ldap://synology-nas.local:389     # Hostname (unverschlüsselt)
#   ldaps://synology-nas.local:636    # Hostname (verschlüsselt)
#   ldap://nas.example.com:389       # FQDN (unverschlüsselt)
#   ldaps://nas.example.com:636      # FQDN (verschlüsselt, empfohlen)
LDAP_URL=
LDAP_BASE_DN=dc=synology,dc=local
LDAP_BIND_DN=cn=admin,dc=synology,dc=local
LDAP_BIND_PASSWORD=
LDAP_USER_SEARCH_BASE=ou=users,dc=synology,dc=local
LDAP_USER_SEARCH_FILTER=(uid={username})

# Datenbank
DB_PATH=/data/database/notenest.db

# Server
PORT=3000 # Backend-Port (Standard: 3000)
NODE_ENV=development # development oder production

# Dateisystem
DATA_ROOT=/data/users

# NAS-Konfiguration
NAS_TYPE= # synology, generic oder leer
NAS_HOMES_PATH=/data/homes # Pfad zu privaten Ordnern (Container-Pfad!)
NAS_SHARED_PATH=/data/shared # Pfad zu geteilten Ordnern (Container-Pfad!)
USER_MAPPING_MODE=same
# Für lokale Entwicklung: Leer lassen oder nicht verwenden

# Bibelstellen-Referenzen
BIBLE_API_ENABLED=true
BIBLE_API_KEY= # Dein YouVersion API Key hier einfügen (https://scripture.api.bible/)
BIBLE_API_URL=https://rest.api.bible
BIBLE_API_CACHE_TTL=3600
BIBLE_SUPERSEARCH_ENABLED=true
BIBLE_SUPERSEARCH_URL=https://api.biblesupersearch.com/api
BIBLE_LOCAL_PATH=/app/data/bibles # Pfad zu lokalen Bibel-JSON-Dateien

# Logging
LOG_LEVEL=info # error, warn, info oder debug
LOG_FILE=/app/logs/notenest.log # Pfad zur Log-Datei

